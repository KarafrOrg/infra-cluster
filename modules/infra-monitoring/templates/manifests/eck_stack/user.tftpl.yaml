apiVersion: elasticsearch.k8s.elastic.co/v1
kind: Elasticsearch
metadata:
  name: monitoring-elasticsearch
  namespace: ${infra_monitoring_namespace}
spec:
  auth:
    roles:
      - secretName: monitoring-elasticsearch-otel-role
